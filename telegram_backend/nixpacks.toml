[phases.build]
cmds = ["pip install -r requirements.txt"]

[phases.release]
cmds = ["python manage.py migrate --noinput"]

[start]
cmd = "daphne -b 0.0.0.0 -p $PORT telegram_backend.asgi:application"
